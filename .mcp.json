{
  "mcpServers": {
    "context7": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@upstash/context7-mcp@latest"],
      "description": "Live documentation lookup for TipTap, TanStack Query, Auth0, MUI, and other dependencies"
    },
    "playwright": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@playwright/mcp@latest"],
      "description": "Browser automation for testing tile drag-and-resize, mobile layout, and TipTap editor interactions"
    }
  }
}
